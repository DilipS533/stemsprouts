/* Sanity studio intentionally removed.

   This file has been replaced with a placeholder to avoid Sanity runtime/type
   dependencies. The site is now a self-contained static Next.js app. If you
   later decide to reintroduce Sanity, restore the studio directory and
   reconfigure projectId/dataset.
*/

export {};
