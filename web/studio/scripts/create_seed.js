// Seed script removed — Studio was removed per project decision.
// If you reintroduce the Sanity studio later, recreate a seed script that
// converts `web/content/extracted.json` into the expected import format.
